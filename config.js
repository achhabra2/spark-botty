module.exports = {
  // API Token, Room to be monitored by the bot, and webhook URL
  // Are now defined in the environment variables
  token: process.env.TOKEN,
  monitorRoom: process.env.MONITORROOM,
  webhookUrl: process.env.WEBHOOKURL,
};
