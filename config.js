module.exports = {
  token: process.env.NODE_TOKEN,
  monitorRoom: process.env.NODE_ROOM,
  webhookUrl: process.env.NODE_WEBHOOK,
  //any thing else you need to reference in the config such as roomId etc
};
